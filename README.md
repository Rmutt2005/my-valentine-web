# My First Valentine Web 💝

โปรเจคเว็บเล็ก ๆ ที่ผมเขียนขึ้นมาเพื่อ **ฝึกใช้งาน React**
โดยเป็นเว็บแนว Valentine / interactive page ที่มีลำดับขั้นตอน (step)
และการจัดการ state ภายในหน้าเดียว

> โปรเจคนี้เป็นการลองเขียนเล่น ๆ เพื่อฝึก React ยังไม่ได้เน้นความสมบูรณ์ของ UX/UI มากนัก

---

## 🎯 จุดประสงค์ของโปรเจค

- ฝึกใช้งาน React Function Component
- ฝึกใช้ `useState` เพื่อควบคุม state ของหน้าเว็บ
- ฝึกแยก component ออกเป็นส่วน ๆ
- ทดลองใช้ icon library และ styling ด้วย Tailwind CSS

---

## 🧠 แนวคิดการทำงานของหน้าเว็บ

หน้าเว็บนี้จะมี 3 สถานะหลัก:
- `question` – หน้าเริ่มต้น
- `reasons` – แสดงเหตุผลทีละข้อ
- `final` – หน้าสรุปท้าย

ใช้ `useState` เพื่อควบคุมว่า
ตอนนี้ผู้ใช้กำลังอยู่ในขั้นตอนไหน และแสดง UI ตามสถานะนั้น

```ts
const [currentStep, setCurrentStep] = useState<
  "question" | "reasons" | "final"
>("question");

